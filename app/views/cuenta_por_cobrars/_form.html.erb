<%= form_for(@cuenta_por_cobrar) do |f| %>
  <% if @cuenta_por_cobrar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cuenta_por_cobrar.errors.count, "error") %> prohibited this cuenta_por_cobrar from being saved:</h2>

      <ul>
      <% @cuenta_por_cobrar.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :empresa %><br />
    <%= f.text_field :empresa %>
  </div>
  <div class="field">
    <%= f.label :numeroDocumento %><br />
    <%= f.text_field :numeroDocumento %>
  </div>
  <div class="field">
    <%= f.label :monto %><br />
    <%= f.text_field :monto %>
  </div>
  <div class="field">
    <%= f.label :fechaEmision %><br />
    <%= f.text_field :fechaEmision %>
  </div>
  <div class="field">
    <%= f.label :fechaVencimiento %><br />
    <%= f.text_field :fechaVencimiento %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
